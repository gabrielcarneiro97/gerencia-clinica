import React from 'react';
import {
  Layout,
} from 'antd';
import ConfigPlanilhaUpload from './ConfigPlanilhaUpload';

export default function ConfigMain(): JSX.Element {
  return (
    <Layout>
      <ConfigPlanilhaUpload />
    </Layout>
  );
}
